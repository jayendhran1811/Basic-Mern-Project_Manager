# 🎉 Preview Guide

## Your App is Starting!

### ✅ What's Running:

1. **Backend Server** → http://localhost:5000
   - API endpoints ready
   - MongoDB connection (if MongoDB is running)

2. **Frontend Server** → http://localhost:3000
   - React app will open automatically in browser
   - If not, manually open: http://localhost:3000

---

## 🚀 How to Use:

### First Time:
1. **Register** - Create a new account
2. **Login** - Use your credentials
3. **Dashboard** - See your overview
4. **Projects** - Create your first project
5. **Tasks** - Add tasks to projects
6. **Tracker** - View LeetCode-style stats! 📊

---

## 📱 Pages Available:

- **Login** (`/login`) - Login page
- **Register** (`/register`) - Sign up page
- **Dashboard** (`/dashboard`) - Overview & stats
- **Projects** (`/projects`) - Manage projects & tasks
- **Tracker** (`/tracker`) - Progress tracker with charts

---

## ⚠️ Important Notes:

### MongoDB Connection:
- If you see "MongoDB Connection Error" in backend:
  - **Option 1:** Install & start MongoDB locally
  - **Option 2:** Use MongoDB Atlas (cloud) - update `.env` file

### To Stop Servers:
- Press `Ctrl + C` in both terminal windows
- Or close the terminal windows

---

## 🎨 What You'll See:

- **Beautiful gradient backgrounds** (purple/blue)
- **Smooth animations**
- **Card-based UI**
- **LeetCode-style tracker** with:
  - Circular progress chart
  - Streak counter 🔥
  - Task breakdown charts
  - Statistics dashboard

---

## 🐛 Troubleshooting:

**Backend not starting?**
- Check MongoDB is running
- Check port 5000 is not in use

**Frontend not opening?**
- Wait a few seconds (React takes time to compile)
- Manually open: http://localhost:3000

**Can't register/login?**
- Make sure backend is running
- Check browser console for errors

---

**Enjoy your preview! 🚀**

